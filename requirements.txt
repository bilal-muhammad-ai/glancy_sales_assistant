# API
fastapi
uvicorn[standard]

# Config
python-dotenv
pydantic-settings

# Knowledge base: crawl + chunk
httpx
beautifulsoup4
lxml

# Vector store + Ollama embeddings (via Chroma OllamaEmbeddingFunction)
chromadb
ollama
# LLM (text Q&A)
groq

# Voice bot (Pipecat + browser WebRTC + Deepgram + Groq + Silero VAD)
pipecat-ai[webrtc,deepgram,groq,silero,runner]>=1.8.0
